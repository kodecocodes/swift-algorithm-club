import UIKit
import PlaygroundSupport

PlaygroundPage.current.needsIndefiniteExecution = true
PlaygroundPage.current.liveView = BoardView(frame: CGRect(origin: .zero, size: CGSize(width: 500, height: 550)))
